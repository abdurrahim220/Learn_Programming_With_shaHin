//Write a program that takes two integers as inputs and finds their average
#include<stdio.h>
int main()
{
    float a,b;
    printf("Input two integer number = ");
    scanf("%f%f",&a, &b);

    printf("Average = %.2f\n",(a / b));
    return 0;
}
